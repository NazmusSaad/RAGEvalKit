# Retrieval-Augmented Generation

RAG combines a retrieval step with text generation. A query is used to retrieve relevant passages from a document corpus. Those passages are injected into the prompt to ground the model's response in factual content and reduce hallucination.
